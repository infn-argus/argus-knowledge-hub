apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attachments-data
  namespace: assetmanagement
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
