fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic==2.9.2
python-multipart==0.0.9
requests==2.32.3
pyyaml==6.0.2
pyjwt[crypto]==2.9.0
cryptography==43.0.1
ldap3==2.9.1
# Kerberos (GSSAPI) binding for ldap3 — see LDAP_AUTH=gssapi.
gssapi==1.8.3
markdownify==1.2.3
# Renders a converted drawing to SVG, including paper-space sheets.
ezdxf==1.4.4
# ezdxf's drawing add-on imports PIL for image entities.
pillow==11.3.0
# Reads the engineering breakdown (PBS) workbooks.
openpyxl==3.1.5
# Reads the text of PDF datasheets and documents for guided entry (AI Intake).
pypdf==5.1.0
